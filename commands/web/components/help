#!/bin/bash
#ddev-generated - Do not modify this file; your modifications will be overwritten.

function show_help() {
    echo -e "
${bldblu}DDEV Grunt/BrowserSync for Magento${txtrst}

${txtgrn}Usage:${txtrst}
  ddev grunt ${txtcyn}[command]${txtrst}

${txtgrn}Commands:${txtrst}
  ${txtcyn}init, install, i${txtrst}    Initialize themes from database (interactive)
  ${txtcyn}themes, theme${txtrst}       List configured themes
  ${txtcyn}watch${txtrst}               Start BrowserSync and Grunt watch
  ${txtcyn}help, -h, --help${txtrst}    Show this help message

${txtgrn}Examples:${txtrst}
  ddev grunt init              # Select themes interactively
  ddev grunt themes            # List configured themes
  ddev grunt watch             # Start BrowserSync and Grunt watch

${txtgrn}First Time Setup:${txtrst}
  1. ddev restart
  2. ddev grunt init
  3. ddev grunt watch

${txtgrn}Note:${txtrst} npm packages are installed automatically during 'grunt init'
       if package.json is missing or packages need to be added.

${txtgrn}Files:${txtrst}
  .ddev/grunt/grunt-local-themes.js  # Theme configuration

${txtgrn}Troubleshooting:${txtrst}
  - Ensure Magento is installed and themes are registered
  - Run: ddev exec php bin/magento cache:flush
  "
}
